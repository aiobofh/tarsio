TMPDIR=T:

all: $(TMPDIR)smakefile.src
	@smake -s -f $(TMPDIR)smakefile.src

$(TMPDIR)smakefile.src: smakefile.local
	@type $< >$@

clean: $(TMPDIR)smakefile.src
	@smake -s -f $(TMPDIR)smakefile.src clean
